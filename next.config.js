/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    swcMinify: true,
    images: {
    },
    compiler: {
        styledComponents: true,
    },
};

export default nextConfig
